namespace ThirtyDollarVisualizer.Engine.Text;

public class TextBuffer
{
    // TODO: text buffer will hold the main buffer for all slices generated from it.
    // The idea is TextProvider -> Singleton, TextBuffer -> can have many instances, holds only one character buffer
    // Text slices get a slice of the TextBuffer and modify it. 
}