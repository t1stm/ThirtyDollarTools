<sundex version="1.0">
  <style language="sundex" src="Scenes/Dialogs/Dialogs.snx.ss"/>

  <layout>
    <flex id="track-context-menu" class="dialog-frame">
      <label class="heading-label" value="Duplicate track"/>
      <!-- Prefilled with "<name> copy" by TrackContextMenu.cs after the build. -->
      <text-input id="duplicate-name-input" class="text-field"/>
      <flex class="dialog-actions-compact">
        <!-- No fill class: Cancel here has always been a bare label, unlike the filled
             Cancel the other dialogs use. -->
        <button id="cancel-button" class="dialog-button-shape">
          <label value="Cancel"/>
        </button>
        <button id="duplicate-button" class="dialog-button-light">
          <label class="dark-label" value="Duplicate"/>
        </button>
      </flex>
    </flex>
  </layout>
</sundex>
