// Imports only - see ArrangementPanel.snx.ss. `id opened-track-name` lives in
// Panels.snx.ss with the rest of the bar's vocabulary rather than here, so the root's
// cascade and this sheet set it to the same thing.
import "Scenes/Styles/Controls.snx.ss";
import "Scenes/Styles/Panels.snx.ss";
