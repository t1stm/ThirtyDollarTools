// Imports only - see ArrangementPanel.snx.ss. The name field carries the same
// `opened-track-name` id as the note editor's bar so both read from the one rule in
// Panels.snx.ss; ids are per-component, so the two never collide.
import "Scenes/Styles/Controls.snx.ss";
import "Scenes/Styles/Panels.snx.ss";
import "Scenes/Views/GridViews.snx.ss";
